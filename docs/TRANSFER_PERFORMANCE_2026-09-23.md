# 사진 전송 성능 개선 — 2026-09-23

## 운영 배포 완료 (13:55 KST)
사용자 “배포” 요청으로 운영 반영. Worker `e6f2e61d-6a20-48f1-8159-e8fba0a65b74`, 소스 SHA는 아래 검증값과 동일. Git 커밋/push는 하지 않음.
전체 검사를 재통과한 뒤 배포. 최초 D1 확인의 7403 오류는 정상 계정 확인 후 같은 요청 재시도에서 해소됐으며 적용할 마이그레이션 없음.
운영 로그인/인증 보호/반복·분할 다운로드 목록/사진 없는 폴더 제외 확인. workspace-2YKoMdo-.js의 해시가 검증 빌드와 일치.
배포 전후 일정93/사진456/일정표원본15/도면초안1 및 8GB 한도 동일. 실사용망 전송 성능 측정은 하지 않음.
로그: Git 제외 .sites-runtime/release-transfer-2026-09-23.log, release-transfer-deploy-2026-09-23.log, transfer-release-before.json, transfer-release-after.json.

## 적용 내용
- ZIP 원본 요청을 순차 처리에서 최대 3개 선행 다운로드로 변경. 선언된 파일 크기 합계 32MiB 이내로 버퍼 제한, 기존 큰 파일은 단독 스트리밍. ZIP 순서·폴더 구조·원본 바이트는 유지.
- ZIP 취소·쓰기 실패·원본 읽기 실패 시 진행 중인 요청 취소 및 정리.
- 사진 선택 즉시 미리보기. 기기 임시 저장을 3장씩 수행하고 각 묶음 저장 완료 즉시 기존 3개 병렬 업로드 슬롯으로 전송.
- 전송 중단 시에도 남은 선택 파일의 기기 보관을 마쳐 재시도 가능하게 유지.
- 서버 저장 확인 ID만 한 번에 기기 보관함에서 삭제. 닫힌 미전송 패널은 File 원본 전체 대신 키 개수만 조회; 갱신 알림 150ms 묶음 처리.
- 사진 화질·원본·인증·사용량 한도 유지. 미전송 패널의 수동 재시도 경로는 기존 순차 방식.

## 비교 조건 및 결과
실제 브라우저 IndexedDB + 로컬 HANA API/D1/R2, 합성 JPEG 2,230,001바이트 × 30장.
요청당 150ms 지연을 추가한 통제 실험이다. 운영망/휴대폰 속도 측정이 아니다.
이전/개선 코드 업로드 실행 순서를 두 번째 회차에 뒤집었다. 다운로드는 회차별 이전/개선/개선/이전 순서.

| 평균 지표 | 이전 | 개선 |
|---|---:|---:|
| ZIP 30장 완료 (각 4회) | 6.503초 | 2.581초 |
| 업로드 첫 HTTP 시작 (각 2회) | 175.2ms | 29.5ms |
| 첫 사진 서버 저장 확인 | 649.4ms | 499.2ms |
| 업로드 30장 전체 완료 | 4.179초 | 4.173초 |
| 미전송 목록 전체 재조회 | 31–32회 | 1회 |

ZIP 소요 시간 약 60% 감소. 업로드 전체 처리량의 개선은 입증되지 않았으며, 초기 대기와 기기 저장소 작업 감소가 확인되었다.
모든 업로드 비교에서 30장 저장 / 미전송 0장. 각 회차 ZIP 이전·개선의 크기(66,908,958바이트)와 SHA-256이 일치.
회차 1 ZIP SHA: 1037c48b78bbb448717ed40ca47343ffbbbfdd8af8ee7e5b06b0c0bc0485c26f
회차 2 ZIP SHA: 09c04fa69079fbb198ce1c6afae655ce4d1955c367374e6c6f62b225493f24e3
회차 간 UUID/이름이 달라 해시가 다르며, 비교는 같은 회차의 동일 원본에 대해 수행했다.
측정 원자료/도구는 Git 제외 .sites-runtime/transfer-speed/round-1.json, round-2.json, server.mjs, page.mjs에 보관.

## 검증
- node scripts/verify.mjs: 단위 109개, 통합 8개 묶음, 타입, 린트(오류 0, 기존 경고 15), 빌드 통과.
- 검증 소스 SHA-256: 73320add3496de379cc02a07036d4cbdf00a1e11f20014c9abda912aca140e5e
- 신규 테스트: 병렬 수·메모리 예산·순서·실패/취소·대형 파일 스트리밍 및 저장 준비/전송 타이밍.
- 실제 HANA 화면에서 추가 3장 선택 → 즉시 미리보기 → 사진 30→33장, 저장 완료 → 미전송 0장 확인.
- git diff --check 통과.

## 상태
위 배포 완료 기록이 최신 상태이며, 미커밋 working tree에 배포 소스가 남아 있다. 이전 Worker 6daeafe0-5a3e-4884-a588-2b63957801f6에서 갱신됨.
로컬 http://127.0.0.1:4175/ 실행, .wrangler/reliability-qa 격리 저장소.
두 전송 성능 검증 지역의 123장 합성 사진을 로컬 테스트용으로 남겼다. 운영 데이터 변경 없음.
