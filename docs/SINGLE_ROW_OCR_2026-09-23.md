# 한 건짜리 일정표 인식 수정

2026-09-23 로컬 구현 후 13:13 KST 운영 반영 완료. 배포 결과는 [운영 배포 기록](RELEASE_ZIP_OCR_2026-09-23.md)을 참고하세요. Git 커밋·push는 아직 수행하지 않았습니다.

## 원인과 수정

- `tableLines`가 가로 경계선을 최소 4개 요구해 머리글과 일정 한 행만 있는 표(경계선 3개)를 거부했습니다. 실제 제공 사진으로 기존 `Table not detected` 오류를 재현했습니다.
- 병합된 비고 머리글에는 주소/메모 사이의 세로선이 없으므로 전체 표 높이로 세로선 비율을 계산하면 짧은 표에서 이 구분선이 누락됩니다. 제공 사진에서도 기존 기준으로는 7개, 본문 기준으로는 8개의 세로 경계선이 검출됐습니다.
- 머리글과 한 행을 허용하고, 세로선의 연결 비율은 일정 본문 높이를 기준으로 계산하도록 수정했습니다. 숫자 열의 폭 검사와 빈 표 거부는 유지합니다. OCR 모델이나 외부 AI 서비스를 추가하지 않았습니다.

## 검증

- 새 `tests/table-lines.test.mjs`의 단일 행/큰 병합 머리글 회귀 검사 2개가 수정 전 실패하고 수정 후 통과했습니다. 일반 여러 행 표와 비일정·빈 표 거부도 검사합니다. 테스트는 합성 픽셀만 사용합니다.
- 사용자가 제공한 `명장2_9.23.jpg` 원본으로 지역·조사 날짜·번지·건물/호수·시간·이름·연락처·메모의 모든 필드를 대조했습니다.
- 로컬 브라우저의 실제 일정표 사진 선택 경로에서도 1건과 모든 필드가 일치했습니다. 원본 사진 및 연락처를 저장소에 복사하거나 커밋하지 않았고, 운영에는 전송하지 않았습니다. 일정 저장 버튼을 누르지 않은 검토 화면 상태입니다.
- `node tests/table-ocr.mjs`: 기존 합성 12건 일정표의 모든 번지, 지역, 날짜, 경고 없음 확인.
- `node scripts/verify.mjs`: 단위 101개(23파일), 통합 8개, 타입 검사, 린트 오류 0, 빌드 통과. 기존 린트 경고 15개는 남아 있습니다.
- 검증 소스 SHA-256: `02f3847341b7a50a28de7a0de437362580bd41987828857dd33ac8f18fa387b8`.
- 전체 검증 로그: Git 제외 `.sites-runtime/single-row-ocr-verify.log`.

로컬 테스트 서버는 `http://127.0.0.1:4175/`, 격리 데이터 `.wrangler/reliability-qa`로 실행 중입니다. 이 사진과 합성 표를 검증했으며, 기울거나 손상된 모든 표의 인식을 보장하는 변경은 아닙니다.
