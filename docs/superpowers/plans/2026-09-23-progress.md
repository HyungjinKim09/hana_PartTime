# SDD ledger — plan: docs/superpowers/plans/2026-09-23-reliability.md

Baseline: external-cloudflare HEAD 56caaeb, 6 tracked dirty entries and 4 untracked application/test files from prior user work. Type check and 85 unit tests passed on prior review with Python path and UTF-8 configured. App lint: 13 errors/15 warnings. Two consistency failures reproduced with synthetic Miniflare data.

Pre-flight: Tasks 1/3 share upload ID; Task 1 revision affects Task 4 folder rows and Task 5 backup. Task 2 ticket issuance is consumed by Task 5; Task 4 aggregate triggers must handle restore inserts. All new migration numbers are reserved sequentially 0013/0014/0015.

Ruling: approved report is binding scope; implement inline without repeating approval. Existing dirty checkout is the required baseline; preserve it. Default budget stays 8GB, increases require explicit admin UI action. Production deployment is not assumed from a request to implement review findings.

Task 1: in progress.

Task 1: server response-loss retry/concurrency/mismatch/deleted receipt + stale edit integration RED→GREEN. Type check and 6 upload tests passed; current build passed. Conflict comparison UI added; final browser validation pending.
Task 2: unsafe GET reproduced; POST/origin/raster changes implemented, validation underway.

## Final execution record

Tasks 1–6 implemented locally. Task 2 security tests passed; strict origin POST is checked before ticket creation and raw documents cannot enter schedule preview. Tasks 3/4 added durable queue, cancellation, paginated reads/aggregate triggers and admin-only cost-aware capacity. Task 5 added SHA-verified directory backup, hidden idempotent restore staging, atomic publish/cancel fencing, cleanup and 200MiB photo-only ZIP parts. Task 6 removed contact seed, corrected documentation/lint, and unified verification/deployment gates.

Fresh independent reviewer found two concrete issues: persistence failure trapped camera originals, and cancellation could falsely report restore complete. Fixed direct-upload fallback + per-capture original download and final publication transition verification. Follow-up reviewer reported no blockers. Additional upload publication requires active folder atomically. CLI verification caught the token stream's budget error being flattened to503; fixed it to preserve429. Browser verification caught Vinext Link prefetch errors; disabled unnecessary prefetch for full-page account/home links.

Final npm run verify:97 unit cases,6 integration suites,tsc,app lint0errors,production build passed. Existing lint warnings about intentional native image display remain. Real browser: network interruption + reload + IndexedDB retry confirmed; stale edit conflict/merge/save confirmed;390px no horizontal overflow;desktop backup/ZIP part UI confirmed;final console no new errors. Native folder picker/real phone termination/quota/optical lens checks remain device acceptance limits, not claimed as executed.

Local server is running port4175 with isolated .wrangler/reliability-qa data and synthetic account. No production call/deployment/Git push was performed. Read docs/RELIABILITY_COMPLETION_2026-09-23.md and dist/build-provenance.json for the final handoff.
