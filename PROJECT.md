# HANA 동작 규약

## 현장 작업

- 데스크톱은 정보 확인·다운로드, 모바일은 정보 확인·촬영이 우선입니다. 방문 전 확인 사항은 항상 표시합니다.
- 날짜 보기는 구역 → 날짜 → 기존 일정 폴더입니다. 주소 보기는 구역 → 일반/구분건물 → 주소 → 빌라 호수 또는 아파트 동·호수이며 날짜로 나누지 않습니다.
- 검색 결과는 주소별 모아보기의 폴더와 호수 수를 표시합니다. 지도 버튼은 주소로 카카오맵/네이버 지도 앱을 엽니다.
- 모바일 기본 액션은 일정 추가·일정표 사진 등록이며 다운로드는 더보기에 있습니다.

## 촬영·업로드

원본 바이트를 보존하고 3장씩 병렬 업로드합니다. 별도 썸네일 업로드는 만들지 않습니다. 선택한 원본 Blob으로 즉시 화면에 표시하고 서버 저장 상태는 별도로 보여 줍니다. 저장되지 않은 사진은 기기 임시 보관과 재시도를 제공합니다. 실패한 사진을 같은 작업 번호로 재전송해도 중복 저장되지 않습니다.

사이트 카메라는 오른손 가로 촬영 기준으로 미리보기를 유지하고 저장할 때만 왼쪽 90° 회전합니다. 기본 카메라에서 고른 파일은 돌리지 않습니다. 초광각 렌즈 제공 여부는 기기/브라우저에 달려 있습니다.

## 일정·도면

일정표는 JPEG/PNG/WEBP 시그니처를 확인하고 이미지 요소 안에서만 미리 봅니다. 한국어·영어 OCR 결과는 저장 전 수정할 수 있습니다. 오전중/오후중/아무때나를 그대로 보존합니다. 추가한 수기 일정은 일일보고의 추가일정에 포함하며 지번은 3자리로 표시합니다. 보고는 복사만 제공합니다.

도면 사진은 현장 사진과 구분합니다. 자동 인식은 검토할 초안이며 내부 구분선은 점선, 문·창문은 추가하지 않습니다. m 단위 치수와 편집선을 저장하고 Visio로 내보낼 수 있습니다. 조사 기록과 도면은 revision으로 오래된 기기의 덮어쓰기를 차단합니다.

## 인증·저장

공용 계정과 임의 세션 쿠키를 사용합니다. `ADMIN_SETUP_KEY`는 계정 설정/복구와 저장 한도 변경에 필요합니다. OAI 헤더나 `SITE_OWNER_EMAIL`은 관리자 권한의 근거가 아닙니다. 모든 자료는 검증된 세션의 owner 범위로 조회합니다. 팀원끼리는 같은 자료 편집 권한을 공유합니다.

D1은 기록과 메타데이터, 비공개 R2는 원본을 저장합니다. 폴더 통계는 트리거로 유지하며 사진은 60장씩 읽습니다. 전체 ZIP은 분할 선택을 지원하고, 목록이 변경되면 이전 분할 목록으로 진행하지 않습니다. 티켓 생성은 POST+동일 Origin으로 제한하고 읽기 예산은 실제 원본 요청 때만 차감합니다.

삭제는 일정과 포함 자료를 제거합니다. 일반 ZIP은 현장 사진만 포함하므로 완전한 보존에는 별도 전체 백업을 사용합니다. 전체 백업에는 일정표 원본·도면·편집 기록도 포함하고, 해시 검증 후 복원 구역을 일괄 공개합니다. 미완료 복원·중단된 업로드의 임시 파일은 명시적으로 정리할 수 있습니다.

검증 및 배포 절차는 README.md, 구현 근거는 docs/superpowers/plans/2026-09-23-reliability.md에 기록합니다. 이 변경은 로컬에서 검증하며 원격 배포·Git 업로드와 운영 자료 삭제는 별도 작업입니다.
