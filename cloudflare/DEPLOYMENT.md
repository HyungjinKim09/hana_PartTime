## 2026-09-23 로컬 보완 작업 (아직 운영 미반영)

현재 배포 절차와 동작 규약은 `README.md`와 `PROJECT.md`를 우선한다. 아래 날짜별 메모는 당시의 이력이다.

- `npm run verify`: 타입·앱 린트, 모든 단위/Worker 통합 검사와 빌드를 동일한 절차로 실행한다. 배포 스크립트도 이 검증을 먼저 수행한다.
- 신규 D1 마이그레이션: 0013 업로드 재시도·기록 revision, 0014 폴더 집계·관리자 저장 한도, 0015 복원 작업. 기존 일정과 사진은 유지한다.
- 기본 제한은 8GB이며 관리자 키와 비용 확인 후 앱 저장 상한을 바꿀 수 있다. 유료 요금제를 자동 변경하지 않는다.
- 현장 사진은 원본 그대로 병렬 업로드한다. 별도 썸네일 생성은 중단된 상태를 유지한다. 미전송 원본을 기기에 임시 저장하고 서버 확인 후 제거한다.
- 일반 사진 ZIP은 현장 사진만 포함하며 200MB 분할을 지원한다. 전체 백업은 별도 기능으로 일정표·도면·기록까지 검증해 보관한다.
- Git revision·소스 해시·검증 시각은 dist/build-provenance.json, 배포 시 Worker 버전은 .sites-runtime/deployments.jsonl에 기록한다. 롤백은 Worker만 되돌리며 D1 자료를 자동 복구하지 않는다.
- 이번 구현·테스트는 로컬 합성 데이터만 사용했다. 실제 연락처 seed 코드는 제거했으나 Git 과거 이력은 재작성하지 않았다.

# Hanaparttime 독립 운영 배포

이 브랜치는 웹 화면, API, D1, R2를 본인 Cloudflare 계정에 직접 배포한다.
실행 중 ChatGPT Sites/Vercel/Supabase를 호출하지 않는다. OCR 엔진과 한국어·영어 모델도 같은 사이트에서 제공한다.

## 확인된 상태

- 독립 관리자 인증과 기존 기능을 로컬 D1/R2 통합 테스트로 검증했다.
- 2026-09-17 본인 Cloudflare 계정에 D1·비공개 R2·Worker 배포 완료: https://hanaparttime.isacc7224.workers.dev
- 사용 한도: 합계 8GB, 최근 32일 쓰기 5만 회·읽기 50만 회. 앱 사용 제한이며 계정 전체의 과금 차단은 아니다.
- 실제 주소에서 독립된 로그인 세션 2개, 5,763,023바이트 PNG 업로드·다운로드, ZIP CRC·SHA-256, 일일보고와 저장량 복구를 확인했다. 검증용 사진은 삭제했다.
- 로컬 관리 키와 최초 공용 로그인 정보는 `.deploy-secrets/`에 저장하며 Git에서 제외된다.
- 기존 사이트의 실제 사진·비고·진행 상태 이전과 실물 휴대폰 검증은 아직 수행하지 않았다.
- 기존 운영 사이트는 변경하거나 삭제하지 않았다.

## 계정 연결 후 배포

1. Node.js 22.13 이상에서 기존 잠금 파일로 패키지를 설치한다.
2. `node node_modules/wrangler/bin/wrangler.js login`으로 본인 계정에 연결한다.
3. `node node_modules/wrangler/bin/wrangler.js d1 create hanaparttime-db --location apac`으로 데이터베이스를 만든다.
4. `node node_modules/wrangler/bin/wrangler.js r2 bucket create hanaparttime-photos --location apac`으로 비공개 사진 버킷을 만든다. 공개 버킷 접근은 켜지 않는다.
5. `cloudflare/deployment.example.json`을 `cloudflare/deployment.json`으로 복사하고 실제 계정 ID와 D1 ID를 넣는다. 이 파일은 Git에서 제외된다.
6. `npm run deploy:production`을 실행한다. 빌드, 타입 검사, 통합 테스트 후 DB 마이그레이션과 Workers 배포를 순서대로 수행한다. 로컬 검증만 하려면 `npm run deploy:production -- --check`를 사용한다.
7. 출력된 `https://hanaparttime.<계정 하위 도메인>.workers.dev`에서 `/account/setup`을 열고 생성된 관리 키로 공용 아이디/비밀번호를 설정한다. 관리 키는 `.deploy-secrets/production.json`에만 저장하며 일반 사용자에게 공유하지 않는다.
8. 실제 새 URL에서 로그아웃 상태 접근 차단, 두 기기의 공용 로그인, 5MB 이상의 사진 원본 업로드·다운로드, ZIP 및 일일보고를 확인한다. 관리자 암호 변경 시 기존 세션도 폐기된다.

`hanaparttime.com` 같은 도메인은 별도 소유권과 구매가 필요하다. 위 workers.dev 주소는 계정 하위 도메인이 결정된 후 확정된다.

## 기존 자료 이전

먼저 기존 사이트에서 전체 ZIP을 내려받고 `/api/library`의 일정·비고·진행 상태와 각 폴더의 사진 목록을 인증된 세션으로 백업한다. 사진만 ZIP에 담겨 있으므로 ZIP만으로 비고와 상태를 복원했다고 간주하면 안 된다.

대상 DB에는 일정 ID, 지역·날짜, 번지·호수, 연락처, 일정 메모, 비고, 상태를 함께 옮긴다. R2 원본을 복사하고 원본의 바이트 수와 SHA-256을 대조한 뒤 사진 메타데이터를 기록한다. 일정표 원본도 전체 ZIP에서 별도로 복원한다. 원본 파일 이름이 같아도 사진 ID는 유지하고, 대상 기존 데이터를 덮어쓰지 않는다.

기존 로그인 세션과 로그인 시도 기록은 이전하지 않는다. 데이터 소유자 값은 새 `site_account.owner`와 일치하도록 설정해야 한다. 이전 전후 지역·날짜·폴더 수, 원본 사진 수·크기·해시, 비고·상태를 모두 확인한 후 사용 주소를 전환한다. 기존 자료는 검증 완료 전 삭제하지 않는다.

## 비용과 제한

### 앱에서 적용하는 사용 제한

- 사진과 일정표 원본을 합쳐 8,000,000,000바이트(십진 8GB)까지 저장한다.
- 최근 32개 UTC 날짜 동안 R2 쓰기 50,000회, 읽기 500,000회까지만 허용한다. 월초에 즉시 초기화하지 않으므로 청구 주기 경계에도 여유를 둔다.
- 미리보기·원본 다운로드는 각각 읽기를 사용하고, ZIP은 포함하는 원본 파일 수만큼 시작 전에 읽기를 예약한다. 한도가 부족하면 ZIP 응답을 시작하지 않고 429 안내를 반환한다.
- ZIP은 브라우저에서 생성한다. `/api/export`는 파일 경로·크기와 1시간 유효한 일회용 다운로드 목록(JSON)을 반환한다. `/api/export/<token>`은 현재 로그인·소유자·세션 버전을 확인한 후 예약된 원본 한 개를 전달하며 토큰은 재사용할 수 없다. 이 경로는 `cloudflare/worker.ts`에서 R2 스트림을 직접 반환해 무료 Worker의 대용량 파일 처리 CPU를 줄인다.
- PC Chrome·Edge에서는 저장 위치를 선택하고 ZIP을 디스크에 스트리밍한다. 해당 기능이 없는 브라우저는 완성된 ZIP을 다운로드하며 256MB까지 지원한다. 더 큰 범위는 폴더별로 나누거나 PC Chrome·Edge를 사용한다. 다운로드 중 이 화면을 유지해야 하며, 누락·크기 불일치·취소 시 불완전한 ZIP 저장을 중단한다.
- 업로드 전에 D1에서 용량을 원자적으로 예약한다. 동시에 올린 요청, 일정표 사진, 실패 후 정리가 확인되지 않은 파일도 용량에 포함한다. 삭제가 R2에서 확인된 뒤에만 용량을 돌려준다. 실패·취소 요청의 작업 횟수는 돌려주지 않는다.
- 한도 도달 시 해당 기능만 차단하며 기존 자료를 자동 삭제하지 않는다. D1 사용량 기록에 실패하면 R2 읽기·쓰기도 진행하지 않는다. 로그인한 보관함 화면에서 사용량을 볼 수 있다.
- **Cloudflare 계정 전체의 과금 상한이 아니다.** 다른 버킷, 대시보드·S3 도구·다른 Worker의 직접 접근, 적용 전 요청, 메타데이터에 없는 기존 객체는 이 집계 밖이다. 전용 비공개 Standard 버킷에만 적용하고 공개 URL·별도 쓰기 경로를 활성화하지 않는다. 기존 데이터를 외부 도구로 이전할 때에는 객체별 사용량 원장도 대조해야 한다.
- 용량 예약은 안전을 위해 과다 집계될 수 있다. 실패한 업로드의 고아 객체·원장을 자동 삭제하거나 원장만 초기화하지 말고, 실제 R2 객체와 대조한 뒤 정리한다.

2026-09-17 PC 검증: Windows 경로를 지원하도록 통합 테스트를 수정했다. 테스트 실행 시 Python 디렉터리를 프로세스 PATH에 추가하고 `PYTHONIOENCODING=utf-8`을 설정한다. `node --test tests/r2-budget.test.mjs`는 실제 Miniflare D1/R2에서 동시성·기간 경계·실패 복구를 검증한다. `npm run deploy:production -- --check`는 빌드·타입 검사·전체 저장 통합 테스트·배포 사전 검사를 실행한다.

**이 변경 이후에는 project의 소스를 빌드해 배포한다.** 상위 `deploy/` 폴더의 예전 빌드에는 사용량 제한이 없으므로 그 파일로 재배포하지 않는다.

무료 범위를 목표로 하고 요금제 업그레이드는 하지 않는다. Cloudflare Workers Free는 일 10만 요청과 요청당 10ms CPU 제한이 있으며, R2 Standard는 월 10GB-month 저장량, Class A 100만 회·Class B 1천만 회 무료 범위를 제공한다. R2는 무료량을 넘으면 과금될 수 있고 활성화 시 결제 수단을 요구할 수 있다. 이 단계가 나오면 사용자 설정이 필요하다. 대규모 ZIP의 CPU 사용량은 실제 계정에서 확인해야 한다.

- https://developers.cloudflare.com/workers/platform/pricing/
- https://developers.cloudflare.com/r2/pricing/

배포 스크립트는 계정 ID 확인 없이 배포하지 않으며, 요금제 변경·결제 수단 등록·이전 사이트 삭제를 수행하지 않는다.

2026-09-17 ZIP 운영 검증: 폴더 사진 29장(93,749,953바이트), 전체 사진·일정표 31개(94,272,969바이트)를 내려받아 ZIP CRC와 개별 원본 SHA-256을 모두 대조했다. 실제 브라우저의 전체 다운로드 버튼으로 생성한 ZIP도 동일 SHA-256을 확인했다(파일 저장 API 없는 브라우저 경로). 운영 ZIP 원본 전달은 CPU 1~4ms로 정상 완료됐다. PC 네이티브 저장 대화상자 조작은 자동화 검증 범위 밖이며 스트림의 정상 완료·오류·취소는 테스트로 검증했다.

2026-09-17 다운로드 범위 수정: 건물 화면의 위쪽 버튼도 현재 건물을 선택한다. 건물 ZIP은 `건물/사진`, 날짜 ZIP은 `날짜/건물/사진`이며 ZIP 파일명도 선택한 건물 또는 날짜를 사용한다. 완료 후 `다시 다운로드`로 매번 새 목록을 발급받아 반복 저장할 수 있다. 내부 원본 토큰의 일회용 보호와 다운로드 사용량 집계는 유지한다. 통합 테스트에서 같은 건물을 두 번 요청하고 원본 길이를 대조하며 날짜·건물 경로와 파일명도 검증한다.

2026-09-17 수기 일정 등록: 보관함 상단 `일정 직접 추가`는 현재 지역·날짜를 미리 채운다. 사진 없이 지역·날짜·번지와 선택 정보(호수, 시간, 이름, 연락처, 주소, 메모)를 입력하고 여러 행을 저장한다. `/api/schedules`의 JSON 요청도 기존 스키마 검증과 인증·출처 검사를 거치며, 기존 지역·날짜·번지·호수는 그대로 보존한다. 수기 입력은 R2 업로드 및 schedule_imports 원본 기록을 만들지 않는다. 사진 등록은 기존 multipart 방식으로 유지한다. 통합 테스트로 추가·재시도·새 날짜·권한·기존 사진/비고 보존 및 R2 사용량 불변을 검증했고, 운영 브라우저에서 자동 입력과 중복 저장, 390px 모바일 입력창을 확인했다.

2026-09-17 폴더 삭제와 탐색: 현재 건물·날짜·지역의 `폴더 삭제`는 삭제 범위·사진 수를 표시하고 확인 후 영구 삭제한다. 날짜/지역의 일정표 원본과 마지막 건물 삭제 시 해당 날짜 일정표도 정리한다. D1 0008은 삭제 중인 폴더의 업로드를 차단하고 기본 일정 초기화를 1회로 기록하여 삭제한 폴더가 재생성되지 않게 한다. 삭제는 10개 원본씩 고정 대상 목록으로 이어서 처리하고 원본 삭제가 확인된 항목마다 진행을 기록하므로 재시도 가능하다. 클라이언트의 해시 URL과 방문 기록에 상위 폴더의 실제 위치를 저장해 브라우저 Back과 상위 폴더 버튼을 지원한다. 비고 변경을 버리지 않기로 하면 편집 상태를 유지한다.

2026-09-17 일일보고 추가일정: 수기로 생성한 일정은 `manual_added`로 구분하고 기존 건물 분류 뒤 `추가일정` 제목 아래 1번부터 출력한다. 형식은 `1. 번지(건물·호수) - 상태 / 특이사항`이며 실제 저장 상태를 사용한다. 공백 비고는 `특이사항 없음`으로 표시한다. 새 수기 입력의 건물·호수 띄어쓰기는 `unit_display`에 보존하고 동일 일정 판별용 unit 정규화는 유지한다. 기존 일정에 대한 재등록은 출처를 바꾸지 않는다. 0009는 UUID 일정 중 원본 일정표 기록과 대응하지 않는 행을 기존 수기 일정으로 보정한다. 과거 수기 일정이 나중에 일정표에도 실린 경우의 최초 출처 및 이미 제거된 띄어쓰기는 과거 데이터만으로 복원할 수 없다.

2026-09-17 OCR 개선: 설치된 한국어/영어 모델의 4.0.0_best_int 버전을 자가 호스팅하고 IndexedDB cachePath를 hana-ocr-best-v2로 분리한다. 지역·날짜를 못 읽으면 표 위의 텍스트 줄을 분리해 단일 줄 모드로 재인식한다. 번지 칸은 누락 시 다른 분할 모드로 재시도하며, 사직통처럼 오독한 동 접미사는 표 제목 지역의 어근과 정확히 일치하는 경우에만 보정하고 안내한다. 동 이름 없이 숫자만 읽은 행도 경고한다. 비고 줄바꿈 유지 및 원본 대조 안내 추가. 테스트 사진은 사용자 로컬 파일로만 입력하며 Git에 포함하지 않는다. tests/photo-ocr.mjs의 --expect 옵션으로 외부 기대값 JSON을 대조할 수 있다.

2026-09-17 촬영 묶음 업로드: 건물 안 `촬영`에서 받은 사진을 BatchCamera의 메모리 목록에 모으고 `추가 촬영`으로 이어 찍는다. 미리보기에서 개별 사진을 뺀 후 `N장 업로드`를 눌러 일괄 전송한다. 성공한 항목은 제거하고 실패한 File만 보관해 재시도한다. 모달을 닫아도 같은 건물에서는 목록을 유지하며, 폴더 이동·페이지 종료에는 미업로드 사진 보호를 적용한다. 임시 사진은 서버나 기기에 영구 저장하지 않으므로 업로드 완료 전에 페이지를 종료하지 않는다. 모바일 실제 카메라 UI는 기기의 기본 촬영 화면을 사용한다.
운영 브라우저에서 3개 파일 선택→1개 제거→닫기/재열기→2개 업로드를 검증했다. 전송 전 서버 사진0개, 첫 시도에서 한 건의503 오류를 주입한 후 실패1개만 남고 재시도 결과 서버2개, 요청 파일 순서 first/third/third로 성공 파일 중복 업로드 없음. 390px에서 미리보기2열 확인. 임시 검증 지역 및 사진2개를 모두 삭제하고 로그아웃했다. 전체 빌드·타입·저장 통합 테스트·배포 dry-run 통과. 운영 버전 3dbee8cd-96d0-4b43-9a44-e50b304e8dcd. 실제 휴대폰 카메라 촬영 동작은 별도 기기 확인 대상이며 브라우저 검증은 캡처 입력의 파일 선택 이벤트로 수행했다.

2026-09-17 도면 사진 기능 (로컬 작업, 미배포): 건물 화면의 현장 사진/도면 사진 선택으로 별도 목록에 등록한다. 다중 파일 및 묶음 촬영, 미리보기, 개별 원본 다운로드/삭제를 공통 지원한다. photos.kind는 photo/drawing으로 제한하며 기존 행은 photo로 보존한다. 종류를 생략한 기존 업로드도 photo다. ZIP은 기존 현장 사진 경로를 유지하고 도면만 건물/도면/ 아래에 넣는다. 같은 저장량/요청 한도와 폴더 삭제 정리 경로를 사용한다. 촬영 대기 또는 업로드 재시도 항목이 있으면 종류 변경을 막아 잘못된 분류로 재시도되지 않게 한다. 향후 배포 시 drizzle/0010_photo_kind.sql 적용이 필요하다. 사용자 요청에 따라 현재 원격 마이그레이션 및 배포는 실행하지 않는다. Visio 자동 변환은 이번 구현에 포함하지 않는다.

2026-09-17 사이트 내 자동 도면 편집 (로컬 구현, 미배포): 도면 카드의 `자동 도면 만들기 · 비지오`에서 원본/인식 범위를 확인하고 Cloudflare Workers AI @cf/google/gemma-4-26b-a4b-it로 초안을 만든다. 인식 전 JPEG로 최대1400px 축소, 본문900KB 제한. 손글씨/점선 인식은 오류가 있을 수 있으므로 실선·점선/글자/좌표/전체 미터 치수를 편집하고 저장한다. 내부선은 추정임을 표시하며 문·창문은 만들지 않도록 요청한다. 도면과 치수만 남도록 인식 범위를 조절할 수 있다.
- 신규 0011_drawing_drafts.sql: 사진별 도면 JSON, revision 기반 동시 수정 충돌 방지, 사진 삭제 트리거로 초안 정리, 사이트 공통 UTC 일20회 AI 요청 제한(실패도 포함). 사진 소유권·종류·삭제 상태 및 CSRF를 확인한 뒤 호출한다. 모델 원문은 스키마 검증하고 HTML/SVG로 실행하지 않는다.
- `vite.config.ts` AI binding 추가. Workers Free 모델 사용, 유료 플랜/결제 업그레이드 없음. 무료 계정 한도 초과 시 Cloudflare가 요청을 중단한다. 앱의20회 제한은 계정 전체 AI 사용량 보장이 아니며 다른 앱 사용량은 별도다.
- 로컬 확인 시 `wrangler dev` 기본 로컬 Worker + 원격 AI binding을 사용한다. `--local` 강제 옵션은 AI에 `Binding AI needs to be run remotely` 오류를 유발한다. `--remote`로 Worker를 올릴 필요는 없다. 테스트 DB/R2는 .sites-runtime/drawing-state로 운영과 분리했다.
- VSDX는 브라우저에서 표준 ZIP32 OPC/XML로 생성한다. 이미지를 붙여 넣지 않고 각각의 선·글자를 Visio Shape로 내보내고, 미터 치수 및1:50 배율을 기록한다. 선 endpoint/width/angle/geometry 관계를 수식으로 연결한다. 자체 테스트로 ZIP CRC, XML, 한글, 치수, 점선, 수정 수식을 검증하고 Python vsdx로 재열었다. 이 PC에 Visio는 없어 Microsoft Visio 실제 렌더링은 미검증이다.
- 실제 제공 도면을 Cloudflare AI로 검증: 전체 사진 첫 시도는 폭/높이 및 구분선 오독. 도면 부분으로 잘라 재시도 시23선/19글자, 전체12.4m/6.7m 반환. 이는 특정 예제 결과이며 자동 인식 정확도 보장은 아니다. 개인 원본/응답은 .sites-runtime 아래만 두며 Git에 포함하지 않는다.
- 미래 배포 시 0010,0011 로컬 변경을 원격 DB에 먼저 적용해야 한다. 사용자 요청 전에는 배포·원격 마이그레이션을 실행하지 않는다.
참고: https://developers.cloudflare.com/workers-ai/models/gemma-4-26b-a4b-it/ , https://developers.cloudflare.com/workers-ai/platform/pricing/ , https://learn.microsoft.com/en-us/office/client-developer/visio/visio-file-format-reference
로컬 브라우저 최종 검증: 원격 AI binding으로 실제 초안 생성, 폭 오독11.4를12.4로 수정 저장 후 재열기 유지. 390px 모바일 수평넘침없음. 브라우저가 생성한 VSDX53개 객체/53,282바이트의 ZIP CRC/XML 및 vsdx파서검증 통과. 자동화 브라우저 파일저장은 TXT 대조군도 취소되어 생성 Blob을 추출해 검증했으며, 실제 파일저장 완료 및 Microsoft Visio 실행검증은 구분해 남겨둔다. 최종 빌드/타입 통과, 도면단위테스트4개 및 전체저장통합통과. 운영미배포.

도면 충실도 수정: 기존 AI 벡터 재구성은 실제 사진과 방 배치가 달라 사용하지 않는다. 이제 AI는 글자·치수만 읽고, 실제 선 좌표는 브라우저의 이미지 처리로 추출한다. 원본 좌표계/인식 범위를 초안과 함께 저장하며 원본 겹쳐 보기를 지원한다. 예전 초안은 재인식해야 하며 자동 덮어쓰지 않는다. 흐린 점선 누락, 글자 오검출, 치수 불명확은 남아 있어 검토 후 저장해야 한다. 그림 부분만 범위에 포함할수록 유리하다. 단위7개/타입/빌드/전체 저장 통합 검증 통과. 운영 미배포 유지.

2026-09-17 ZIP 범위 변경: 건물/날짜/지역/전체 다운로드 모두 현장 사진(kind=photo)만 포함한다. 도면 원본, 비지오, 일정표 원본은 포함하지 않는다. 위의 과거 도면 ZIP 포함 설명은 이 변경으로 대체된다. 도면 인식은 사용자 평가상 실패 상태이며 완료된 기능으로 간주하지 않는다. 로컬만 수정, 운영 미배포.

2026-09-18 도면 재수정(로컬): 사진 방향을 먼저 판정한 후 글자 인식(자동인식당 AI 요청 최대2회, 일20요청 공유). source.rotation과 revision=2를 저장하고 원본 및 편집 도면 좌표를 동일하게 처리. 글자 영역으로 원본 픽셀을 지우던 처리 제거, 약한 선의 관찰 증거와 실제 교점으로 선 복구, 교점 단위 실선/점선 판별. 새 사진의 주요 선 배치를 검증하되 손글자 오독은 남는다. 특정 사진 도형 하드코딩 없음. 현장사진 전용 ZIP 정책 유지. 배포하지 않음.


## 2026-09-18 도면 기능 배포 대상 (이전 작업 메모보다 우선)

- 도면 사진 분리 등록, 원본 비교·범위/회전 조정, 자동 초안·선/글자 편집·저장 및 VSDX 내보내기를 포함한다. 0010/0011 마이그레이션이 필요하며 기존 사진은 현장 사진으로 유지한다.
- AI는 방향과 글자·치수만 읽는다. 선은 최대960px 이미지에서 브라우저가 추출하며 실제 픽셀 증거와 연결 관계로 짧은 점선 경계를 보존한다. source.revision=3이며 이전 초안은 새 인식 안내를 표시하고 자동 덮어쓰지 않는다.
- 15도 이내 기울기는 수평·수직으로 보정하고 짧은 돌출을 교점에 맞춘다. 명백한 대각선과 연결된 단차를 보호한다. 기존 도면은 직각·돌출 보정 버튼과 되돌리기를 지원한다.
- AI 요청은 자동 인식당 최대2회, 사이트 공통 UTC 일20회 제한이며 실패도 집계한다. 손글씨/희미한 선의 완전한 인식은 보장하지 않으므로 원본과 대조 후 저장한다.
- 건물/날짜/지역/전체 ZIP에는 현장 사진만 포함한다. 도면·비지오·일정표 원본은 제외하고 도면의 개별 다운로드는 유지한다.
- 도면 관련17개 테스트는 작은 점선 enclosure, 글씨/모눈 오검출, 회전, 직각/돌출 보정, VSDX ZIP/XML/미터 치수를 검증한다. Microsoft Visio 자체 실행은 이 PC에 설치되지 않아 미검증이다.
- 개인 사진, AI 응답, 운영 DB 백업, 로그인·관리 키는 Git 제외 디렉터리에만 보관한다. 로컬 시험 데이터는 운영에 복사하지 않는다.

2026-09-18 사용자 배포 승인 후 운영 반영 완료: D1 0010/0011 적용, Worker 버전 `c62bdf22-1879-4f10-98b0-c2eda62673b1`, https://hanaparttime.isacc7224.workers.dev . 위의 미배포 메모는 당시 이력이며 현재 상태는 배포 완료다. 배포 전 DB 백업, 도면17개 테스트, 빌드·타입 검사·전체 저장 통합 검사를 완료했다. 로컬 검사 중 Windows 포트 부족은 연결 정리 후 재검사로 통과했고 일시적인 D1 7403 응답은 동일 계정·DB 재요청으로 정상 적용됐다. 운영에서 비로그인 접근 차단, 로그인, 도면/현장 사진 업로드 분리, AI binding 연결 여부, 초안 저장·재열기·수정 충돌, ZIP 도면 제외, 원본 바이트 및 검증 자료 삭제를 확인했다. 운영 AI 실제 추론은 별도 호출하지 않았으며 로컬 원격 AI 검증 결과와 구분한다. 기존 운영 사진은 유지했고 로컬 테스트 사진/초안은 운영에 이전하지 않았다.


## 2026-09-18 목록 속도 및 사용량 최적화
- 0012_photo_thumbnails.sql: 사진별 thumbnail_key 추가. 새 업로드는 기기에서 긴 변480px/최대64KiB JPEG 미리보기를 생성해 원본과 별도로 저장한다. 생성 실패·지원하지 않는 형식·한도 초과 시 원본 업로드를 중복 재시도하지 않고 원본 표시로 대체한다.
- 유료 이미지 변환 서비스는 추가하지 않는다. 썸네일당 최초 R2 쓰기1회와 소량 저장공간이 추가되며 기존8GB/읽기/쓰기 예약 한도에 포함한다. 개별/폴더 삭제 시 썸네일도 삭제해 공간을 반환한다. 원본 및 ZIP에는 미리보기를 넣지 않는다.
- 원본 inline/썸네일은 private,no-cache + ETag를 제공한다. 재검증마다 로그인·소유권·삭제상태를 확인한 후 변경없으면304를 반환하여 R2 읽기와 본문 재전송을 생략한다. 다운로드는 no-store 유지한다. 공개 CDN 캐시를 사용하지 않는다.
- 폴더 재방문은 메모리 내 최대20개/60초의 목록 캐시를 사용한다. 직접 새로고침과 업로드·삭제·비고 저장은 캐시를 무효화한다. 자동 갱신은 활성화된 화면에서60초 간격, 포커스 복귀는 마지막 조회30초 이후로 제한한다. 다른 기기의 변경은 갱신 시 반영된다.
- 폴더/사진/사용량 조회를 병렬화하고, 도면 편집기를 필요할 때 지연 로드한다. 미리보기 생성 실패 시 원본은 유지되며 브라우저가 HEIC를 디코딩하지 못하면 기존 HEIC 원본 표시를 유지한다.
- API 통합 테스트: 썸네일 인증/CSRF/형식, 재시도/동시저장, 저장공간 집계,304시R2읽기 불변,원본/ZIP보존,개별·폴더삭제 정리. 로컬 브라우저:480px 썸네일 실제표시,374KB→38KB 예제,폴더 재방문 API0회 확인.

운영 반영 완료: Worker `005dc44c-f839-4a47-b200-5019c78be426`, D1 0012 적용. 빌드·타입·전체 저장 통합 검사 통과. 기존 사진30장에 미리보기를 생성해 원본98,696,483bytes 대비818,254bytes(목록용 이미지 전송량 약99.17% 감소), 추가 R2 저장 약0.82MB/최초 쓰기30회. 원본을 변경하지 않았다. 운영 폴더 API 3회171~193ms, 예제 원본2,781,445bytes의 미리보기30,199bytes 확인. 재검증304가R2읽기를 소비하지 않음 확인. 운영 브라우저29장 목록의 이미지URL은 모두썸네일이며 원본요청0회,초기 도면편집기 로드0회. 이는 측정 환경의 관측이며 전체 화면속도가99% 빨라졌다는 의미는 아니다.
