# Cloudflare Worker secrets: configure via wrangler secret, never commit values.
# ADMIN_SETUP_KEY: at least 32 random characters, used only by the administrator.
# SITE_DATA_OWNER: preserve the existing owner namespace when moving deployments.
# For local development only, place synthetic values in ignored .dev.vars.
